import { Box } from "@mui/system";
import { Layout, Search } from "ui/components";

export const Home = () => {
  return (
    <Layout>
      <Box>
        <Search/>
      </Box>
    </Layout>
  );
};